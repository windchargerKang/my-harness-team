# 产品规则

## 核心原则

- **用户可见行为**必须在 spec 中明确写出
- 除非 change 明确说明，否则优先保持向后兼容
- **不允许出现静默行为变化**
- 错误码、提示文案、接口返回结构变更必须明确说明

## 验收标准

一个 change 只有在满足以下条件时才算通过：

1. **行为与 OpenSpec 工件一致**
2. 必要测试通过
3. Review 没有未解决的严重问题
4. 关键风险已说明

## 接口变更规则

### 变更类型

| 类型 | 说明 | 要求 |
|------|------|------|
| 增 | 新增接口 | 明确参数、返回值、错误码 |
| 删 | 删除接口 | 确认无调用方，建议 deprecated 后再删除 |
| 改 | 修改现有接口 | 明确影响范围，必要时提供兼容方案 |
| 查 | 查询类接口 | 明确返回字段含义 |

### 兼容性处理

- **向后兼容**：新增字段、废弃字段处理
- **版本管理**：如有多端，需协商接口版本

## 错误码规范

- 错误码需要在本文档中维护
- 修改错误码含义需通知调用方
- 禁止返回含义不明确的错误码

---

## 错误码表

| 错误码 | 含义 | 处理建议 |
|--------|------|----------|
| (待补充) | | |
