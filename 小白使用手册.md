# Harness Suite 小白使用手册

> 适合第一次使用 Harness Suite 的同学。照着做就能跑通。

---

## 0. 先搞清楚一件事：命令在哪里执行？

`/harness-team-*` 这类命令，**不是在系统终端（zsh/bash）里执行**。

要在 **Cursor/Claude 对话输入框**里执行，例如直接发：

```text
/harness-team-setup
```

如果你在终端里敲：

```bash
/harness-team-setup
```

会报：`no such file or directory`，这是正常现象。

---

## 1. 安装（推荐方式）

在你的业务项目根目录执行：

```bash
curl -fsSL "https://github.com/windchargerKang/my-harness-suite/archive/refs/heads/main.tar.gz" \
  | tar -xz && \
bash my-harness-suite-main/install.sh --target "$(pwd)" && \
rm -rf my-harness-suite-main
```

安装成功后：
1. 重启 Cursor/Claude 会话
2. 在对话框输入 `/harness-team-setup`

---

## 2. Team 完整流程（推荐）

1. `/harness-team-setup`：初始化 team 目录与模板
2. `/harness-team-propose <需求名>`：需求澄清与提案
3. `/harness-team-plan <change-id>`：方案设计与任务分解
4. `/harness-team-prd <change-id>`：产出可执行 PRD 与验收合同
5. `/harness-team-apply <change-id>`：按任务执行开发
6. `/harness-team-verify <change-id>`：统一质量验证
7. `/harness-team-fix <change-id>`：修复失败项（按需循环）
8. `/harness-team-review <change-id>`：并行评审
9. `/harness-team-archive <change-id>`：归档变更
10. `/harness-team-knowledge <change-id>`：沉淀经验

一键模式：
- `/harness-team-run <需求名|change-id>`
- `/harness-team-run <需求名|change-id> --quick`
- `/harness-team-run <需求名|change-id> --strict`

查看状态：`/harness-team-status <change-id>`

全自动（最少交互）：
- `/harness-team-autopilot <需求描述> --quick`
- `/harness-team-autopilot <需求描述> --strict`

tmux 可视化 worker：
- `/harness-team-workers start <change-id>`
- `/harness-team-workers attach <change-id>`

---

## 3. 每个命令怎么用（最简版）

## `/harness-team-setup`

**作用**：初始化 team 协作目录与模板。

**你会得到**：
- `openspec-team/changes/`
- `openspec-team/specs/`
- `openspec-team/templates/`

---

## `/harness-team-propose <需求名称>`

**作用**：创建需求提案，定义范围和验收标准。

**你会得到**：
- `openspec-team/changes/<change-id>/proposal.md`

---

## `/harness-team-plan <change-id>`

**作用**：做技术方案和任务拆解。

**你会得到**：
- `plan.md`
- `tasks.md`
- 风险与验证策略

---

## `/harness-team-prd <change-id>`

**作用**：把 plan 固化为可执行 PRD 与验收合同。

**你会得到**：
- `prd.md`（含 Given/When/Then 验收条目）

---

## `/harness-team-apply <change-id>`

**作用**：按 `tasks.md` 逐步实现。

**特点**：
- 按 milestone 推进
- 禁止无边界扩需求
- 记录执行偏差

---

## `/harness-team-verify <change-id>`

**作用**：执行 lint/test/build/验收验证。

**你会得到**：
- `verify-report.md`
- 是否进入 fix-loop 的判定

---

## `/harness-team-fix <change-id>`

**作用**：修复 verify/review 的失败项。

**流程**：
- 修复后回到 `/harness-team-verify`
- 直到门禁全部通过

---

## `/harness-team-review <change-id>`

**作用**：并行评审（规范、架构、SQL、可维护性）。

**你会得到**：
- `review-report.md`（P0/P1/P2 分级）
- 发布门禁结论

---

## `/harness-team-archive <change-id>`

**作用**：把已通过门禁的变更归档。

---

## `/harness-team-knowledge <change-id>`

**作用**：提炼可复用经验（问题-原因-修复-预防）。

---

## 4. 新手推荐顺序（照抄即可）

在 Cursor 对话里依次输入：

```text
/harness-team-setup
/harness-team-propose 用户登录
/harness-team-plan <上一步生成的change-id>
/harness-team-prd <change-id>
/harness-team-apply <change-id>
/harness-team-verify <change-id>
/harness-team-review <change-id>
/harness-team-archive <change-id>
/harness-team-knowledge <change-id>
```

如果验证或评审失败，补一条：

```text
/harness-team-fix <change-id>
```

---

## 5. 常见报错与解决

## 报错：`zsh: no such file or directory: /harness-team-setup`

**原因**：你在系统终端执行了 Slash 命令。

**解决**：去 Cursor 对话框里执行 `/harness-team-setup`。

---

## 报错：命令不生效

**排查**：
1. 是否刚安装后未重启会话
2. `.claude/settings.json` 是否存在 commands 配置
3. `.claude/skills/` 下是否有 `harness-team-*` 目录

---

## 6. 给小白的最后建议

- 先按流程走，不要跳步骤。
- 没有 `proposal/plan/tasks` 就不要直接开写。
- 每次改完先 verify，再 review。
- 评审通过再 archive，再沉淀 knowledge。